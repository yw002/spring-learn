<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="io.github.yw002.springlearn.mybatis.mapper.OrderMapper">
    <!-- 配置了 <package name="xx"> 后，实体类支持大小写随意的别名【Order/order/oRDer】   -->
    <select id="findByUid" resultType="Order">
        SELECT *
        FROM tb_order
        WHERE user_id = #{id}
    </select>
</mapper>